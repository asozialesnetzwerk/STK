;; -*- mode: lisp -*-

(tuxkart-kart
 (name        "Eviltux")
 (model-file  "eviltux.ac")
 (icon-file   "eviltuxicon.rgb")
 (shadow-file "generickartshadow.rgb")
 (groups      "standard") 
 (rgb         0.3 0.1 0.6)

)
;; EOF ;;
